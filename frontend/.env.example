# Frontend Environment Variables
# Copy to .env and configure as needed

# ===========================================
# API Configuration
# ===========================================
# Development: Leave empty (uses proxy to localhost:8000)
# Production: Set to your API server URL
VITE_API_URL=

# ===========================================
# API Key (for production only)
# ===========================================
# Must match the API_KEY in backend .env
# Leave empty for development with DISABLE_AUTH=true on backend
VITE_API_KEY=
